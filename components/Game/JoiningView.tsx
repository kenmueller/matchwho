import { Text, StyleSheet } from 'react-native'
import theme from '../../lib/theme'

const GameJoiningView = () => (
	<Text style={{ color: theme.white }}>GameJoiningView</Text>
)

const styles = StyleSheet.create({})

export default GameJoiningView
