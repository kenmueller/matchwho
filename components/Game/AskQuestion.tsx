import { Text, StyleSheet } from 'react-native'

import theme from '../../lib/theme'

const GameAskQuestion = () => (
	<Text style={{ color: theme.white }}>GameAskQuestion</Text>
)

const styles = StyleSheet.create({})

export default GameAskQuestion
